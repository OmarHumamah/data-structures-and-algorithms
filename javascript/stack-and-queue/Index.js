'use strict';

const Stack = require('./Stack');
const Queue = require('./Queue');

let stack = new Stack();
let queue = new Queue();

//stack.push(1);

//console.log(stack.peak());
module.exports = {stack, queue}